{
    "deploy-scheduled-tasks": {
        "path": "scripts/deploy-scheduled-tasks.ps1",
        "outputMode": "convention",
        "bigOutput": "full",
        "timeout": 120,
        "context": "system-ok",
        "description": "Deploy Task Scheduler tasks from GitHub task sets"
    },
    "kill-windows-nags": {
        "path": "scripts/kill-windows-nags.ps1",
        "outputMode": "convention",
        "bigOutput": "full",
        "timeout": 120,
        "context": "system-ok",
        "description": "Silences Windows nags (OOBE, Edge, OneDrive, tips, Copilot) without uninstalling anything"
    }
}
